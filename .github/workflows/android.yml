name: Android CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: set up JDK 11
      uses: actions/setup-java@v3
      with:
        java-version: 11.0.15
        distribution: 'temurin'
        cache: gradle

    - name: Grant execute permission for gradlew magueule
      run: cd Front-end-mobile && chmod +x gradlew
    - name: Build with Gradle
      run: cd Front-end-mobile && ./gradlew build
  apk:
    name: Generate APK
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: set up JDK 11
        uses: actions/setup-java@v1
        with:
          java-version: 11.0.15
      - name: Build debug APK
        run: cd Front-end-mobile && bash ./gradlew assembleDebug --stacktrace
      - name: Upload APK
        uses: actions/upload-artifact@v1
        with:
          name: app
          path: cd Front-end-mobile && app/build/outputs/apk/debug/app-debug.apk